apiVersion: v1
kind: Service
metadata:
  name: sonarqube-java-demo
  labels:
    app: sonarqube-java-demo
spec:
  type: LoadBalancer
  selector:
    app: sonarqube-java-demo
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
