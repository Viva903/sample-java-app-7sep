# Jenkins base URL, no trailing slash, e.g. http://localhost:8080
JENKINS_URL=

# Jenkins username for API auth
JENKINS_USER=

# Jenkins API token (Manage Jenkins > Users > <user> > Configure > API Token)
JENKINS_API_TOKEN=
